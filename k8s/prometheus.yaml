apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: chatbot-monitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: chatbot
  endpoints:
    - port: http-metrics
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
